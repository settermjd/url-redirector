{
  "name": "settermjd/url-redirector",
  "description": "This is a simple package that simplifies the process of performing redirects from one absolute URL to another. It's not meant to be, in any way, sophisticated.",
  "type": "library",
  "license": "BSD-3-Clause",
  "authors": [
    {
      "name": "Matthew Setter",
      "email": "matthew.setter@gmail.com",
      "homepage": "https://matthewsetter.com",
      "role": "Developer"
    }
  ],
  "require": {
    "php": "^5.6 || ^7.0"
  },
  "require-dev": {
    "phpunit/phpunit": "^6.2"
  },
  "autoload": {
    "psr-4": {
      "Redirector\\": "src/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "RedirectorTest\\": "test/"
    }
  },
  "scripts": {
    "check": [
      "@cs-check",
      "@test"
    ],
    "test": "phpunit --colors=always",
    "test-coverage": "phpunit --colors=always --coverage-clover clover.xml"
  }
}
